{
  "name": "kamalakar-media-cms-infra",
  "version": "0.1.0",
  "private": true,
  "description": "IaC for the Kamalakar Heart Centre media CMS — Cognito, DynamoDB, Lambda, API Gateway, CodeBuild publish pipeline. Deploy with: npx cdk deploy",
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "diff": "cdk diff",
    "destroy": "cdk destroy"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.1133.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.0"
  }
}
